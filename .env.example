# reCAPTCHA v3 — site key PÚBLICA (se expone al frontend)
# Crear en: https://www.google.com/recaptcha/admin
PUBLIC_RECAPTCHA_SITE_KEY=6Le...

# Las variables del Worker se configuran en Cloudflare Dashboard,
# NO aquí (son secretas):
#   RECAPTCHA_SECRET   — secret key de reCAPTCHA
#   GITHUB_TOKEN       — personal access token (repo → issues:write)
#   GITHUB_REPO        — "avelrom/saldeahi"
